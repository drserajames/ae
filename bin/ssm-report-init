#! /usr/bin/env python3
"""
Scaffold an SSM/seasonal WHO CC report working directory (subdirs, static
templates, and a date-substituted report.json / setup.json).

Thin wrapper over ae.report.init — see that module for options. Requires the ae
python package on PYTHONPATH (run as `PYTHONPATH=build:py bin/ssm-report-init ...`).
"""
import sys
from ae.report.init import main

if __name__ == "__main__":
    sys.exit(main())
