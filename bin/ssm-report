#! /usr/bin/env python3
"""
Assemble an SSM/seasonal WHO CC report from a report.json + figure PDFs.

Thin wrapper over ae.report.cli — see that module for options. Requires the ae
python package on PYTHONPATH (run as `PYTHONPATH=build:py bin/ssm-report ...`).
"""
import sys
from ae.report.cli import main

if __name__ == "__main__":
    sys.exit(main())
